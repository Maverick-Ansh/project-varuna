# Paper section: learned street-graph path planning (FloodGNN)

Drop-in section for the workshop paper (markdown mirror + LaTeX below). Numbers marked
`TODO-COLAB` come from `scripts/run_gnn_colab.py --train --ablation --transfer --eval`
(one T4 session; see `RUNBOOK_GNN.md`). Everything else is already measured and committed.

**Positioning.** The paper's diagnosis (§DEM-uncertainty) is that at 60 m in flat terrain,
*where* water pools is uncertain even when *how much* is robust. FloodGNN turns that into a
planning tool anyway: instead of trusting any single raster, it learns a per-street flood
ranking that (a) answers at any rainfall in one forward pass, (b) routes evacuations that
provably cross less water than shortest paths, and (c) transfers across cities — the model
sees only local, per-graph-standardized features, never a city identity. The transfer
experiment (train on flat Patna, test zero-shot on hilly Bengaluru and vice versa) is the
learned-model analogue of the flat-vs-hilly co-location finding.

**Already-measured facts usable in the text now:**

| fact | value | provenance |
|---|---|---|
| Patna street graph | 37,017 nodes / 79,928 directed edges | `varuna/gnn/graph.py` on `artifacts/patna` |
| dataset generation | 9.4 s/area on CPU (12 storms + 3 planner configs) | `dataset.build_dataset` log |
| CPU smoke (hidden 48, 3 layers, 24 ep, Patna only) | held-out-storm edge AUC **0.856**, wet-RMSE 0.161 m | `train_gnn` log 2026-07-06 |
| smoke routing @140 mm, 150 random OD pairs | shortest 2,297 m wet/route → GNN 1,735 m (+24% detour) → oracle 1,172 m (+37%) | `artifacts/gnn/gnn_report.json` (smoke) |
| model size | ~90k params (hidden 96 · 4 layers ≈ 330k) | `model.py` |

## LaTeX

```latex
\subsection{Learned path planning on the street graph}

Both planning surfaces above route on graphs with hand-set costs: storm drains via a
reverse Dijkstra whose flow-direction cost ($\ell + 2400\,\Delta z^{+}$\,m) encodes a fixed
uphill aversion, and evacuation implicitly via raster overlays that must be recomputed per
storm. We replace the hand-set cost with a learned one. \textbf{FloodGNN} is an
edge-conditioned message-passing network (4 rounds, 96-dim states, $\sim$330k parameters,
plain PyTorch) over the OSM street graph, FiLM-conditioned on rainfall so a single model
covers the continuous storm range. Node features are deliberately local and standardized
per graph --- elevation anomaly, pit-ness, grade, flow accumulation, permanent water,
SAR waterlogging frequency, building/road density --- so nothing identifies the city.
Supervision is generated by the twin itself: the emulator's depth fields for a 20--240\,mm
sweep, sampled at street nodes and edges, and the drain planner's per-street conveyed
volume at three (rain, inlet-count) configurations. Wet targets carry a $\times$20 loss
weight (flooding is sparse), and validation holds out \emph{storm sizes}, not nodes.

FloodGNN answers three questions. \emph{Risk}: per-street flood depth at any rainfall in
one forward pass (edge AUC \todo{TODO-COLAB} on held-out storms, vs 0.856 for a 24-epoch
CPU smoke model trained on Patna alone). \emph{Routing}: cost-inflated Dijkstra
($\ell \cdot (1 + 8\min(d,1.5) + 25\cdot[d>0.3\,\text{m}])$) yields evacuation routes that
cross \todo{TODO-COLAB}\,m of flooded street per route versus \todo{}\,m for the
flood-ignorant shortest path and \todo{}\,m for an oracle using the emulator directly ---
at a \todo{}\% mean detour. \emph{Placement}: the flow head reproduces the planner's
trunk-drain ranking (Spearman $\rho=$ \todo{TODO-COLAB}) without running it.

\textbf{Cross-city transfer.} Trained with Bengaluru held out entirely, FloodGNN ranks
flooded Bengaluru streets at AUC \todo{TODO-COLAB} zero-shot (and \todo{} for the reverse
direction), against \todo{} when trained on all cities. A no-message-passing ablation
(layers $=0$, the same features through a per-node MLP) reaches only \todo{}, confirming
that neighbourhood structure --- not raw topography --- carries the signal. This extends
the co-location analysis of \S\ref{sec:uncertainty} to learned models: street-level flood
knowledge is substantially city-transferable even where cell-level locations are not,
because the graph aggregates exactly the local basin context that single cells lack.

Deployment note: the checkpoint (1.3\,MB) ships inside the dashboard image; the
\texttt{/api/route} endpoint answers from the GNN where present and from the raster
emulator otherwise, reporting its backend. A full-graph risk query takes
\todo{TODO-COLAB}\,ms on CPU versus \todo{}\,ms for the emulator pipeline it amortizes.
```

## Experiment checklist for the Colab session (fills every TODO above)

1. `--data` all four areas (expect: bengaluru flow labels may be sparse — disclose).
2. `--train` (production, all areas) → report `gnn_report.json` per-area AUC / wet-RMSE /
   flow-Spearman / routing / ms-per-query.
3. `--ablation` → layers 0 vs 2 vs 4 table.
4. `--transfer` → the four headline numbers: AUC(bengaluru | trained w/o bengaluru),
   AUC(patna | trained w/o patna), each vs the all-cities model.
5. `--figures` → `auc_vs_rain.png`, `route_demo_patna.png`, `pred_vs_true.png`,
   `train_curve.png` (copy the keepers into `paper/figures/`).
6. Optional stretch: n-inlet sensitivity of flow labels; MC-dropout uncertainty on edge
   depths (model already takes `dropout=`).
