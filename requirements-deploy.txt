# Backend deploy deps (HF Spaces Docker). CPU torch via the extra-index in the Dockerfile.
# Heavy libs are required so the live /whatif, /canals, /optimize endpoints work (not just the
# static bundle endpoints).
torch==2.4.1
numpy<2.2
scipy>=1.11
pandas>=2.0
rasterio>=1.3
scikit-image>=0.22
matplotlib>=3.7
pillow>=10
requests>=2.31
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2
