# Artifact bundle / outputs
floodtwin/
*.tif
*.pt
*.html
!web/index.html
*.png
alerts_today.csv
recharge_sites.csv
dig_plan.json
canal_plan.json
costbenefit.json
validation_scores.json
gw_levels.csv

# transient viz arrays
_*.npy
_*.npz

# Local config (keep config.example.yaml)
config.yaml

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ipynb_checkpoints/
*.egg-info/
.venv/
venv/

# OS
.DS_Store
Thumbs.db
# csi_net run outputs: the JSON is the evidence and is COMMITTED - a Kaggle kernel is
# ephemeral and results that live only there are results that get lost. Logs are not.
csi_net/results/*.log

# twin simulation cache: recomputable in ~28 CPU-minutes, 14 MB, not evidence
csi_net/results/twin_grids.npz
